# 极简方案 · 断言台账（独立留档）

> 目的：`docs/design/极简方案-定稿.md` §5.4 曾多次被**外部回写**（编辑后数分钟被还原为旧内容）。
> 本文件为台账的**独立留档**，内容与定稿 §5.4 同步；若两者不一致，**以本文件 + git 提交记录为准**。
> 状态口径：`待实施` / `已实施(附证据)` / `部分已实施` / `顺延` / `人工核对`。**无证据 = 未实施**。
> 最近更新：2026-09-11（B4-b 主体：材料换轨，研读+browse → 整章原文压缩）

## B1 · 配置与能力面（本批已落地）

- `A13-1`｜默认值 / 模型选项 / 显示名 / 回退默认统一为 `deepseek-flash`｜**已实施**｜证据：`tests/config/deepseekModelUnification.test.js`「A13-1」；触点：`src/config/apiConfig.js`（三字段默认值）、`src/modules/SettingsModule.vue`、`src/modules/GenerateModule.vue`、`src/utils/jsonParser.js`
- `A13-2`｜旧值兼容映射（`deepseek-v4-pro` / `deepseek-v4-flash` → `deepseek-flash`），旧 localStorage 不失效｜**已实施**｜证据：`tests/config/deepseekModelUnification.test.js`「A13-2」两组；实现：`normalizeDeepSeekModelId` + `normalizeModelFields` 接入 `loadConfigSync` / `loadConfig`
- `A13-3`｜生成 / 答案页 / 分析链路无回归｜**人工核对**｜需真机跑「正文 + 答案页」与一次教材分析；配置面单元与全量单测均全绿
- `A13-4`｜模型自动发现不再"优先 pro"，且**为后续新模型留切换入口**｜**已实施**｜① 选择规则 `pickDiscoveredDeepSeekModel`（当前可用则不动；失效才回落 flash；绝不自动切 pro）——证据：`tests/config/deepseekModelUnification.test.js`「A13-4」；② 切换入口：`listDeepSeekModels()` 拉 /models **全量** id 进设置页下拉，日后再出 Pro 旗舰一经云端提供即可手动选用——证据：`tests/config/deepseekModelDiscovery.test.js` 7 用例
- `A14-1`｜三层分层：`最终帽 = min(偏好层帽, 物理层帽)`｜**已实施**｜证据：`tests/config/outputGuardrailLayers.test.js`（默认 64K / 可调 / 被物理层挡住 / 物理更严时以物理为准）；实现：`resolveEngineOutputLimit = min(resolveEngineCapability().maxOutput, resolveOutputCeiling())`
- `A14-3`｜设置页每槽显示"基准 → 校准 → 生效"三值 + 样本数 + "恢复基准"｜**已实施**｜口径=**系数链路**（与生成端 `pickSlot` 同口径）。已覆盖情形：① 手填优先（生效=手填，不被校准静默覆盖）；② 单桶显值 / 多桶显区间+桶数；③ 无校准（"—"+沿用基准）；④ 校准被禁用不计入；⑤ 答案页槽不参与校准（与生成端一致）；⑥ 非当前路径槽标注"仅参考"；⑦ 非法值显示"—"；⑧ 档位/手填/校准变更即时联动。证据：`SettingsModule.vue` 的 `coefViewsByType`/`slotView`/`calCoefText`/`calNote`/`restoreSlotBase`；SFC 编译 + `vite build` 通过；人工核对：设置页目视联动
- `A14-5`｜两处硬编码改为能力表推导｜**部分已实施**｜① 输出护栏规则表已由 `ENGINE_CAPABILITY` 派生（证据：`tests/config/outputGuardrailLayers.test.js` + `tests/config/engineOutputLimit.test.js` 7/7 未回归）；② `MAIN_TOKEN_CEIL`（`useAiGenerator.js`）半条**顺延 B4**
- `A14-6`｜读盘合并改 merge 语义 **或** 生效值改纯推导｜**已实施**（采用"纯推导"分支）｜校准值存独立键 `budgetCalibrationValues`、生成端 `getCalibratedCoef` 直读现算，从不写入 `apiConfig` → 读盘覆盖不涉及校准值
  - 🔴 偏差记录：**未**改 5 处 `Object.assign(apiConfig, …)`。原因：bootstrap 用同步 `loadConfigSync`（原始存档、不归一），异步 `loadConfig` 才做 Key 解密 / `baseUrl` 修正 / `generationSettings` 深合并 / `budgetByType` 规整；"缺则补"会让未归一值胜出 → 回归上述修正
- `A4-9`｜输出护栏；`MAIN_TOKEN_CEIL` 消除｜**部分已实施**｜① `apiConfig` 半已落地（分层求解，64K 保留为偏好层默认，见定稿 §6.1 偏差修订）；② `MAIN_TOKEN_CEIL` 半条**顺延 B4**
- `A14-2`｜优先级：`custom` 不被校准覆盖 / `tier` 可被校准覆盖 / `cap` 硬顶｜**顺延 B4**｜实现点在 `useAiGenerator.js` 的 `pickSlot`；按"同一文件只在 B4 动一次"的批次纪律随写作链一并实施/验证

## B2 · 清理面（本批已落地）

- `A9-1`｜覆盖对账死代码清除，且 src 内零残留引用｜**已实施**｜清单与证据：
  - 移除：`coverageReconciler.js` 的 `reconcileCoverage` / `reconcileCoverageStats` / `coverageNoteOf`（及其专属依赖 `contractOf` / `classifyProbe` / `literalProbeWords` / `groupByChapter` / `wordMatch`）；`coverageContract.js` 的 `COVERAGE_MODES` / `COVERAGE_MODE_DESC`；`useAiGenerator.js` 的 `digestedNames`
  - 保留：`stripHtmlForRecon`——仍被生产侧 `domainReconciler`（域覆盖对账"只报不改"防线）复用，故 `coverageReconciler.js` 不可整文件删，收敛为仅此工具
  - 证据：src 全局搜索上述符号**零引用**（仅剩注释）；定向 6 文件 74 用例通过 + 全量测试全绿 + `vite build` 通过
  - 📌 **新增发现（留 B4 判定）**：`coverageProbe.js` 的 `classifyProbe` / `literalProbeWords` / `groupByChapter` 现已在生产侧无调用（仅测试引用）——是否连带移除，随 B4 写作链重构按"是否仍需缺料诊断"一并定，避免现在删了 B4 又要加回

## B3 · 分析面（本批已落地）

> 落点：分析输出**即锚清单本体**（`knowledgeHierarchy` 第 2 层 = 锚），故第 2 层粒度必须在分析入库时把住。
> 新增 `src/utils/anchorTreeContract.js`（纯函数，可测）：契约校验 + 粒度判据 + 粒度诊断 + 按章分组。

- `A1-1`｜语文低段锚清单不含"最小单位"｜**已实施**｜① 判据进提示词（结构定义内，全学科统一）：第2层=可独立成题/独立教学组织的考点；最小单位（单字/单词条/单符号/单数值/术语碎片）**强制下沉第3层**；语义判据 + 反例（"人/口/手"），**不写数量区间**；② 修正冲突的语文条（生字/生词改为"逐个提取但落 specificConcepts"）；③ 去掉 specificConcepts"最多4个"（防为凑数删减）；④ 校验+诊断兜底。证据：`tests/utils/anchorTreeContract.test.js`
- `A1-2`｜分析入库按锚树契约校验（结构不符不落库）｜**已实施**｜`validateAnchorTree`（**只判结构、不改内容**）接入逐章落库点：不合规 → 不写锚树 + `analyzed=false` + 报错 + 未入库章清单。证据：`anchorTreeContract.js` + `GenerateModule.vue` 落库点 + 测试
- `A1-3`｜锚粒度诊断可观测｜**已实施**｜`diagnoseAnchorTree` / `logAnchorGranularity`（逐章一行）/ `summarizeAnchorGranularity`（多章汇总）接入分析完成处；输出锚数 / 短锚(≤3字)占比 / specificConcepts 条数分布[min·中位·max·空] / 绑定状态分布（生成期）。证据：测试三组诊断用例 + `📐 [锚粒度诊断]` 日志
- `A1-4`｜锚点清单按章分组、章序与原文一致｜**部分已实施**｜`buildAnchorListByChapter` / `formatAnchorListByChapter`（章序=传入锚序；多条锚共用章标题为正常形态；同名去重）；**提示词落位顺延 B4**。证据：测试「A1-4」三用例

## B4-a · 取料/压缩地基（本批已落地）｜B4-b 待实施

> B4 面过大 → 拆两批（按"同一处代码区只改一次"）：**B4-a** 只新增压缩模块（不碰主链）；
> **B4-b** 一次性改完 `useAiGenerator.js` 写作链（移除 browse / 读整章原文 / 前缀顺序 / 答案页 / 语料 / 守卫 / A14-2 / MAIN_TOKEN_CEIL）。
> 新增 `src/utils/textbookCompression.js`（Map→Reduce，纯逻辑 + 注入 `callAI` 便于测试）。

- `A4-2`｜压缩文本保留栏目/章结构｜**已实施（B4-a）**｜提示词硬约束"不得删掉任何栏目"/"保留原文表述与原句"；标题行原样计入正文
- `A4-3`｜压缩文本不含锚点清单式条目｜**已实施（B4-a）**｜**结构杜绝**：API 无锚点入参；Map 提示词不含"锚/考点"字样
- `A4-5`｜压缩输入为纯原文｜**已实施（B4-a）**｜同 A4-3
- `A4-6`｜压缩比按 `mode` 分流｜**已实施（B4-a）**｜`full`→verbatim（保原句/不得改写句式）；命题型→condense（保结构保事实/不得删栏目）
- `A4-7`｜Map 按章/节自然边界；Reduce 按原顺序拼接｜**已实施（B4-a）**｜切节→分批（不跨节超限；单节超限句子级切分并标 part/parts）→程序拼接；`concurrency` 控并发
- `A4-8`｜分节标题只保留原文已有标题，绝不新造｜**已实施（B4-a）**｜标题行形态识别（短行+无句读防误判）+ 原样留在正文；无标题→单节 `title:''`
- `A4-1`｜锚点与压缩文本**章级**可对应｜**部分已实施（B4-a 模块侧）**｜按原文结构切节、每批带 `title`；写作期同现顺序由 B4-b 接线
- `A4-4`｜写作期输入顺序 = 锚点清单 → 压缩原文 → 委托书｜**顺延 B4-b**
- 📌 **B4-a 附带落地**：失败不丢料（某批压缩失败→保留该批原文 + warning）、超窗递归折叠、`estimateTokens/tokensToChars` 与 `CHARS_PER_TOKEN` 同口径
- 证据：`tests/utils/textbookCompression.test.js` 17 用例（分流/切节/不丢正文/分批保序/防假压缩/失败降级/递归折叠/并发上限/统计口径/token 换算）

## B4-b 首批（已落地）｜主链改造主体仍待实施

> B4-b 主链改造面极大，先收口两处**不涉及产品取舍**的小面；主体（移除 browse / 改读整章原文 / 前缀顺序 / 答案页 / 栏目核对）留待下一片。

- `A4-9`｜输出护栏 = `min(物理层, 偏好层)`；`MAIN_TOKEN_CEIL` 固定常量消除｜**已实施**｜① 护栏分层求解（`apiConfig`，证据：`tests/config/outputGuardrailLayers.test.js`）；② `MAIN_TOKEN_CEIL = 98304` 已消除——触顶升级上界改取推导出的 `engineCap`（原常量 98304 **高于**引擎护栏 65536 而形同虚设），护栏探询上移到预算解析之前
- `A14-5`｜两处硬编码改能力表推导｜**已实施**｜① 规则表由 `ENGINE_CAPABILITY` 派生；② `MAIN_TOKEN_CEIL` 消除（同上）
- `A11-1`｜材料供料与 `copyGuard` 语料**不含类型过滤**｜**部分已实施**｜语料已改 = `collectChapterRawText(contentCards)` 整章段级文本（不过滤 type）；供料侧随 B4-b
- `A11-2`｜空/未标注 `type` 段不再被静默丢弃｜**部分已实施**｜共用取料函数不看 type；研读供料链随 B4-b
- 证据：`tests/utils/textbookCompression.test.js` 19 用例（新增「程序直读整章原文」2 组）

## B4-b 主体（已落地）｜余项见下

> 材料来源整体换轨：**研读轮 + browse 按需取片段 → 程序直读整章原文 + 压缩**。
> `useAiGenerator.js` 净减 668 行 / 增 66 行；写作前缀固定为「锚点清单 → 压缩原文 → 素材使用约定/组织方式 → 委托书」。

- `A15-1`｜材料 = 勾选章节完整原文（程序直读）｜**已实施**｜`collectChapterRawText` → `compressOriginalText`；无任何锚绑定取料
- `A15-2`｜browse 机制整体移除（含逐条清单）｜**部分已实施**｜写作主链内全清（工具定义/白名单 4 家/`buildBrowseSystem`/`deriveBrowseParams`/`generateBodyByTextbookBrowse`/未浏览章确认与重试/相关报告项）；残留：设置页 `browseAutoFill` 开关、`generate()` 的 `studyOnly` 预研读缓存（涉 `GenerateModule.vue`）
- `A15-3`｜copyGuard 语料 = 勾选章节原文｜**已实施**｜与供料同源
- `A15-4`｜引用约束按 mode 分流｜**已实施**｜显式补回【素材使用约定】（full 可引用须转写不标出处；其余仅参考、一律自拟）
- `A4-1`｜锚点与压缩文本章级可对应｜**已实施**｜锚清单与压缩原文同章序同现
- `A4-4`｜写作期输入顺序｜**已实施**｜锚点清单 → 压缩原文 → 约定/组织 → 委托书
- `A5-1`｜输出组织以委托书栏目序列为准｜**已实施**｜新增【组织方式】块（+ 委托书既有措辞）
- `A11-1`/`A11-2`/`A11-3`｜供料与语料无类型过滤 / 未标注段不丢弃 / 练习段不得照搬｜**已实施**
- `A12-2`｜点名仅声明范围、组织以栏目为准｜**已实施**｜同【组织方式】
- `A12-1`｜正文不再整栏目漏写｜**人工核对**｜需真机跑一次生成核对栏目完整性
- 守卫保留确认：完整优先判据 / 截断续写链 / 题号连续性校验 / 预算帽与触顶升级 / 思考降级重试 / 针对性重试 / 答案页两次尝试 / 卷级守门 均原样保留
- 证据：`useAiGenerator.js` grep browse/研读 零残留（仅注释）；全量 `npm test` 122 文件 / 1708 用例全绿；`vite build` 通过

## B4-b 余项（已落地）｜剩 A5-2 与 studyOnly 链

- `A6-1`/`A6-2`｜答案页前缀 = 压缩原文（**仅 full**）→ 正文全文 → 委托书（答案规范），**不带锚点清单**｜**已实施**｜`ansPrompt` 重排 + `ansMaterial` 仅 full 拼入
- `A6-3`/`A6-4`｜答案逐题对齐正文、以正文题目为唯一基准｜**已实施**｜答案指令"题号与正文完全一致、与正文同构"；无锚清单 → 无第二套组织
- `A14-2`｜`custom` > 校准 > `tier`；**`cap` 硬顶**｜**已实施**｜区分超限成因：系数/校准导致 → 取 `cap`（`budgetAlert` 明示"按 cap 硬顶收紧"）；范围性（原文量本身 > cap）→ 既有显式升级（受引擎护栏封顶）
- `A15-2`（续）｜`browseAutoFill` 开关与配置键彻底移除｜**已完成**｜设置页整块 UI + `toggleBrowseAutoFill` + `apiConfig` 两个配置键删除，并在保存时剔除旧存档字段；全库仅剩注释与剔除语句
- `A16-1`｜未分析/仅目录章**锚点 = 目录**｜**已实施**｜`buildTocCard` 落 `anchorTree`（章名 + 子标题，深度≤3、去重）→ 进【锚点清单】，不再从覆盖范围消失。证据：`anchorTreeContract.test.js`「A16」两组
- `A17-1`｜"有原文但未分析"章**真原文照压**｜**已实施**｜`buildTocCard(...,cleanRawText)` 随卡携带 `rawText`；`collectChapterRawText` 优先取之。证据：`textbookCompression.test.js`「A17」用例
- `A7`｜缓存边界：分析缓存保留、**研读缓存取消**｜**已实施**｜研读链整体移除（见下），其缓存与状态机一并消失
- **研读链彻底移除（2026-09-11 用户指示：不得再"挂着"）**｜删 `studyOrchestrator.js`/`studyRound.js`/`generationSession.js` + 3 个研读测试；`useAiGenerator` 删 import/`runStudyNow`/`studyOnly` 分支/预研读缓存复位块/`externalStudy`；`GenerateModule` 删「研读素材」按钮与 `startStudyPhase`/`studyCacheRef`/流程引导（4→3 步）。`chatStudyDigestOnce` → **`chatNonThinkingOnce`**（被原文压缩复用，保留）
- 🐛 **交叉验收发现的集成缺陷（已修）**｜`promptLibrary` 的 `{material}` 占位符仍在向模型描述**两条已删除的机制**（"素材经研读消化 + 研读覆盖点名+摘要 + 按范围目录 browse 现取"）——每次委托书都会发出去｜改为新口径（原文以【压缩原文】随本委托注入 / 范围见【锚点清单】/ 约束见【素材使用约定】），同步改测试断言并反向断言不含"研读""browse"
- `A15-2`（续）｜`studyOnly` 空烧调用链｜**已完成**（随研读链整体移除）
- 📌 **记录修复（自检发现）**：A16/A17 这两条决定原**未记入定稿**（讨论过但漏文档）→ 已补记为定稿 §2.5/§2.6 并立断言，避免再次"讨论过却没落地"
- 证据：全量 `npm test` 119 文件 / 1680 用例全绿（-31 为研读专属用例）；`vite build` 通过；提交 `17af5b2` 已推送

## 未实施（后续批次）

- `A5-2`（交付前栏目级完整性核对）｜🔴 **已取消（2026-09-11 用户定）**｜理由：栏目结构是**大类**，其下才是大题/小题；拿大类栏目名做集合比对**漏不掉真正会丢的层**、检不出价值且易误报（模型可合并/改写栏目名，蓝图只给框架、内容交模型）。"断题/漏题"由既有**题号连续性核对**（3 处 `detectBodyNumberingGap`）兜住
- `A4-9` 输出护栏｜**已实施（2026-09-11 用户裁定：成本闸门 64K → 128K，且全推导）**｜① 物理层 = 引擎能力表；偏好层 = **成本闸门**（默认 131072，设置可调）；生效帽 = `min(物理, 闸门)`。② **全推导**：`src/utils/outputQuota.js` 的 `planOutputQuota`（单次帽 = min(引擎帽, 槽帽, 需求×缓冲)；轮次 = ⌈需求÷单次帽⌉−1，至少 1 轮；总额度 = 单次帽×(1+轮次)）+ `nextContinuationBudget`（每轮帽随已产出递减、额度用尽停止）。③ 链上固定量已清除：`MAIN_TOKEN_CEIL`（B4-b 首批删）、`MAX_CONT=Math.min(6,…)`（本次删）。证据：`tests/utils/outputQuota.test.js` 11 例 + `tests/config/engineOutputLimit.test.js` 8 例（含反向断言"物理上限未直接放行"）
- `A14-4`（出厂默认数值调研，B5；需 B4 落地后实测数据）｜待实施
  - 📊 **本机实测基线（2026-09-11 采样，来自应用 localStorage `budgetCalibrationSamples`）**：`practice|split` 9 条、中位产出率 **1.367**（字符/字符，中位输入 5872 字 → 输出 8026 字）、截断 0 条；`summary|once` 1 条、产出率 1.099（5872 → 6455）。对照播种均衡档：`practice.body` 系数 1.55（token/字）⇒ 隐含基线 1.55×1.3=2.02 产出率，**高于实测 1.367 约 47%**；`summary.once` 3.6 ⇒ 隐含 4.68，远高于实测 1.099。→ 结论：有样本的两类型**预算充足且偏宽**（叠加生成端 ×1.25 缓冲后余量约 1.8 倍，cap 24000 远未触顶）；其余 7 类型暂无实测样本，待 B4 后积累
- `A13-3`（真机回归）｜人工核对

## 批次与文件纪律

- B1 配置与能力面：`apiConfig.js`、`SettingsModule.vue`、`GenerateModule.vue`、`jsonParser.js`（+ 新增测试）
- B2 清理面（A9 死代码）：`coverageReconciler.js`、`coverageContract.js`、`useAiGenerator.js` 死变量
- B3 分析面（A1-1~4）：教材分析提示词 + 解析入库校验 + 锚粒度诊断
- B4 写作链路重构（A15/A11/A4-1~8/A5/A6/A12 + A14-2 + 两处 `MAIN_TOKEN_CEIL`/`A4-9` 残留）：`useAiGenerator.js` + 新压缩模块
- B5 数值调研（A14-4）
